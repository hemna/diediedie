============
Installation
============

At the command line::

    $ pip install hpe-openstack-tools

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv hpe-openstack-tools
    $ pip install hpe-openstack-tools
